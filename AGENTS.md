# Agent instructions

This repository is a reusable project template. Replace this sentence with a one-sentence description of the copied project.

## Start here

- Keep this file short. Put a rule here only when it applies to every task in the repository.
- Do not read all project documentation by default. Read linked guidance when the task needs it.
- Add the package manager here only when the project does not use npm.
- Add build and type-check commands here only when they are not clear from the project files.

## Task-specific guidance

- When creating or changing `AGENTS.md`, read [the AGENTS.md guidelines](docs/engineering/agents-md-guidelines.md).
- When creating, updating, superseding, or reviewing an Architecture Decision Record (ADR), read [the ADR template](docs/engineering/adr-template.md) and use the [`write-adr` skill](.agents/skills/write-adr/SKILL.md).
- When committing and pushing repository changes, use the [`commit-and-push` skill](.agents/skills/commit-and-push/SKILL.md).
- When creating a GitHub pull request, use the [`create-pr` skill](.agents/skills/create-pr/SKILL.md).

## Project facts

- Project description: replace the sentence at the top of this file.
- Package manager: add only when it is not npm.
- Build and type-check commands: add only when they are not clear from the project files.
